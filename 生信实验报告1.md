# Mini-BLAST 算法实现实验报告

## A. 测试内容与结果

### ① 输入输出格式

**index_kmers 函数：**
- **输入：** query序列(str)，k值(int)
- **输出：** Dict[str, List[int]] - k-mer到位置列表的映射
- **示例：** `index_kmers("ACGTAC", 3)` → `{"ACG":[0], "CGT":[1], "GTA":[2], "TAC":[3]}`

**ungapped_extend 函数：**
- **输入：** 查询序列q、受体序列s、种子位置(q_pos, s_pos)、评分参数
- **输出：** (best_score, q_start, q_end, s_start, s_end) - 最佳HSP的分数和半开区间
- **示例：** 对种子"ATG"进行X-drop延伸，返回最优局部比对区间

**smith_waterman_local 函数：**
- **输入：** 两个序列q、s，评分参数(match, mismatch, gap_open, gap_extend)
- **输出：** (best_score, q_aln, s_aln) - 最佳分数和带缺口的比对字符串
- **示例：** 输出格式如 `"ACGT-AC"` vs `"AC-TAAC"`

### ② 测试具体内容

**测试用例1 - K-mer索引测试：**
```python
query = "ATGCATGC"
k = 3
结果 = {"ATG": [0, 4], "TGC": [1, 5], "GCA": [2], "CAT": [3]}
```

**测试用例2 - X-drop延伸测试：**
```python
q = "ACGTACGT"
s = "CCGTACGA" 
种子位置: (2, 1) # 都是'G'
延伸结果: 找到最佳HSP区间 [1,7) vs [0,6)
```

**测试用例3 - Smith-Waterman测试：**
```python
q = "ACGT"
s = "AGCT"
结果: 分数=2, "ACGT" vs "AGCT" (1个错配)
```

### ③ 关键细节测试

**X-drop停止条件测试：**
- 当 `best_score - current_score > dropoff` 时正确停止
- 验证在分数大幅下降时算法及时终止，避免过度延伸

**半开区间验证：**
- 返回的区间 [start, end) 符合Python切片约定
- 验证 `q[q_start:q_end]` 和 `s[s_start:s_end]` 长度相等

**仿射缺口转移方程验证：**
- E矩阵：`max(H[i-1][j] + gap_open + gap_extend, E[i-1][j] + gap_extend)`
- F矩阵：`max(H[i][j-1] + gap_open + gap_extend, F[i][j-1] + gap_extend)`
- 新开缺口惩罚更重，延长缺口惩罚较轻

**回溯终止于0验证：**
- Smith-Waterman回溯在遇到H[i][j]=0时正确停止
- 确保只输出局部最优比对，不包含负分区域

### ④ 与BLAST网页版比较

**相似性：**
- 都使用k-mer种子策略快速定位相似区域
- 都采用X-drop延伸优化性能
- 都支持局部比对，适合找相似片段

**差异性：**
- 官方BLAST使用更复杂的评分矩阵(如BLOSUM62)
- 官方BLAST有预计算的统计显著性评估
- miniBLAST简化了算法，主要用于教学演示

## B. 概念解释与比较

### ① 关键概念解释

**X-drop算法：**
X-drop是一种启发式延伸策略，从种子匹配点开始向两边延伸比对。维护当前分数和历史最佳分数，当两者差值超过阈值X时停止延伸。这避免了不必要的计算，提高了算法效率。

**仿射缺口罚分(Gotoh算法)：**
仿射缺口模型认为开启一个新缺口的代价远大于延长已有缺口。数学表达为：
- 缺口总代价 = gap_open + (gap_length × gap_extend)
- 这比线性缺口模型更符合生物学实际情况

**HSP (High Scoring Pair)：**
高分片段对，指两个序列间得分较高的局部比对区域，是BLAST算法的核心输出。

### ② miniBLAST与Smith-Waterman算法关系

**相同点：**
- 都采用动态规划思想
- 都支持局部比对
- 都可以处理仿射缺口罚分

**不同点：**
- **速度：** miniBLAST使用k-mer索引+X-drop，速度更快；Smith-Waterman是精确但较慢的算法
- **精度：** Smith-Waterman保证找到最优解；miniBLAST采用启发式方法，可能错过最优解
- **应用场景：** Smith-Waterman适合小规模精确比对；miniBLAST适合大规模数据库搜索

### ③ miniBLAST与官方BLAST差异

**算法简化：**
- miniBLAST: 简化的k-mer索引，基础的X-drop延伸
- 官方BLAST: 复杂的种子策略，多阶段过滤，优化的延伸算法

**评分系统：**
- miniBLAST: 简单的match/mismatch评分
- 官方BLAST: 生物学意义的替换矩阵(PAM/BLOSUM)

**统计评估：**
- miniBLAST: 仅提供原始分数
- 官方BLAST: 提供E-value、bit score等统计显著性指标

**数据库处理：**
- miniBLAST: 基础的序列处理
- 官方BLAST: 优化的数据库格式、并行计算、内存管理

**实用功能：**
- miniBLAST: 教学演示用途
- 官方BLAST: 生产环境使用，支持多种序列类型、输出格式、Web接口等

## 总结

本实验成功实现了miniBLAST算法的核心组件，包括k-mer索引、X-drop延伸和Smith-Waterman局部比对。通过测试验证了算法的正确性，并深入理解了BLAST算法的工作原理。miniBLAST虽然功能简化，但包含了现代序列比对算法的核心思想，为理解生物信息学算法提供了良好的基础。

**参考资料：**
- BLAST算法官网：https://blast.ncbi.nlm.nih.gov/doc/blast-quick-start-guide/chooseblastprogram.html
- Altschul, S.F., et al. (1990). "Basic local alignment search tool." J. Mol. Biol. 215:403-410.
